version: "3.8"
services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - db-data:/data
    environment:
      - DB_FILE=/data/church_pos.db
      - PORT=5000
    restart: unless-stopped

volumes:
  db-data:
    driver: local